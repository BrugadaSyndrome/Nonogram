{{define "nonogram"}}
    <table class="nonogram">
        {{range .Board}}
            <tr>
                {{range .}}
                    <td>&nbsp;</td>
                {{end}}
            </tr>
        {{end}}
    </table>
{{end}}